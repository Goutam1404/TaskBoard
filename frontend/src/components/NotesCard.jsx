import { Link=} from 'lucide-react'
import React from 'react'
import Link from "react-router"
const NotesCard = ({note}) => {
  return (
<Link to={`/note/$(notes.)id`}
 className='card bg-basecontent'
  )
}

export default NotesCard